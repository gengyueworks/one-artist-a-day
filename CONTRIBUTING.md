# 贡献指南 / Contributing

感谢你对「每天认识一位艺术家」的兴趣！以下是参与方式。

## 如何提名艺术家

1. 在 [Issues](../../issues) 中新建一个 issue
2. 标题格式：`[栏目] 艺术家名`，例如 `[her] 索菲·陶伯-阿普` 或 `[east] 牧溪`
3. 正文说明：
   - 推荐理由（一两句话）
   - 你知道的生卒年、代表作品（如有）
   - 是否有可用的公有领域图片（如有，附链接）

维护者会评估后加入 `data/pool.json` 的对应栏目选题池。

## 如何写一张卡

### 前置条件

1. Fork 本仓库
2. 按 [_meta/artist-card-template.md](_meta/artist-card-template.md) 模板写卡
3. 图片必须来自公有领域或 CC0（见下方「图片红线」）
4. 所有事实性信息（生卒年、作品年份、收藏地）**必须交叉核对两个独立来源**

### 文件命名与路径

```
artists/YYYY/MM/YYYY-MM-DD-slug.md
images/YYYY-MM-DD-slug.jpg
```

- `slug`: 英文小写，用连字符分隔（如 `rembrandt`, `artemisia-gentileschi`）
- `date`: 预计发布日期（须为未来日期或当天）
- `column`: 必须匹配 `data/columns.json` 中定义的七栏之一

### 写作纪律（强制）

提交前逐条自查：

| # | 规则 | 不通过示例 |
|---|------|-----------|
| 1 | 「一句话」不能替换成任何其他艺术家也成立 | "他是十七世纪最伟大的画家之一" |
| 2 | 「为什么是他」含年份/地点/事件至少两个要素 | "他一生创作了许多杰作" |
| 3 | 「看这一张」指出画面具体位置或元素 | "色彩运用大胆而富有表现力" |
| 4 | 「一个细节」宁可空着也不编 | 编造未经验证的轶事 |
| 5 | 禁用词：令人叹为观止 / 不禁让人 / 在那个时代 / 深远影响 / 无与伦比 / 他的一生充满传奇 / 这不仅仅是一幅画 | 出现即重写 |
| 6 | 图片只用 PD 或 CC0 | 使用版权期内图片 |
| 7 | 生卒年/作品年份/收藏地交叉核对两个来源 | 只查了一个网站 |

### 图片红线

| 规则 | 说明 |
|------|------|
| **只存公有领域图** | Met Museum Open Access、AIC Public Domain、Wikimedia Commons PD-only |
| **1955 年后去世 = link-only** | frontmatter 标 `image_license: link-only`，正文放外部链接 |
| **最长边 ≤1600px** | JPEG quality 85，文件 ≤500KB |
| **标注来源** | `image_source` + `image_source_url` 必填 |

### 提交 PR

1. 推送到你的 fork
2. 新建 Pull Request，标题格式：`[栏目] YYYY-MM-DD: 艺术家名`
3. PR 正文简述：
   - 信息来源（哪些网站/书籍）
   - 图片来源 URL
   - 自查结果（上述 7 条是否全部通过）

## 审核流程

1. 维护者检查写作纪律合规性
2. 抽查关键事实（生卒年、作品年份、收藏地）
3. 验证图片授权状态
4. 通过后合并，同时更新：
   - `data/pool.json`（将对应条目 status 改为 `published`）
   - `indexes/` 下四套索引
   - `data/artists.json`

## 其他贡献方式

- **翻译**: 将已有卡片翻译成英文/日文等，存为 `YYYY-MM-DD-slug.en.md`
- **纠错**: 发现事实错误直接提 issue 或 PR，附更正来源
- **索引改进**: 优化 indexes/ 下的分类和交叉引用
- **脚本贡献**: 改进 scripts/ 下的自动化工具

## 行为准则

- 尊重多元文化视角——艺术史不是只有西方
- 对在世艺术家保持尊重——他们不在本项目的收录范围内
- 有争议的事实标注"有争议"，不要隐去不同观点

## 问题？

开 issue 或查看 [FAQ](../../issues) 中已有的讨论。
